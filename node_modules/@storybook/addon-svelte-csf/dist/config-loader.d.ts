import type { Config } from '@sveltejs/kit';
/**
 * Try find svelte config and then load it.
 *
 * @returns
 * Returns the svelte configuration object.
 */
export declare function loadSvelteConfig(): Promise<Config | undefined>;
